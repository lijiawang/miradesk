# Leave empty during local development; Vite proxies API requests to the backend.
VITE_API_BASE_URL=

# Change this when the backend does not run on 127.0.0.1:8000.
VITE_BACKEND_PROXY_TARGET=http://127.0.0.1:8000
